grades:
    app1:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    app2:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    app3:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    app4:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    app5:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    app6:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true
    app7:
        type: float
        subgrades:
            scope: integer
            correctness: integer
            design: integer
            style: integer
        calculation: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
        hide_calculated: true

calculation:
    normal:
        tech: 1

tech:
    grades:
        app1: 1
        app2: 1
        app3: 1
        app4: 1
        app5: 1
        app6: 1
        app7: 1
